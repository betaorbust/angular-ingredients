/**
 * @ngdoc overview
 * @name ingredients
 * @description
 *
 * # ingredients
 *
 * The `ingredients` module provides a collection of usefule services for accomplishing common
 * tasks that people run into when building projects with Angular.
 *
 * Each of the individual components of `ingredients` can be used separately and each comes with
 * its own documentation and tests.
 */

// Initializes the main  module, which all the others rely on.
angular.module('ingredients', []);
